@Library('jenkins-library') _
pipeline{

    agent any
    stages{

        stage('mvn install'){

            steps{
                sh 'mvn clean install'
                script{

                   installMvn.install()
                   echo installMvn.install()
                }
            }
        }
    }
}