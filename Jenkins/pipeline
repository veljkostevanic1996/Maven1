@Library('jenkins-library') _
pipeline{

    agent any
    stages{

        stage('mvn install'){

            steps{
                sh 'cd'
                sh 'cd /home/vstevanic/Maven1'
                script{
                    instalMvn.instal()
                }
            }
        }
    }
}