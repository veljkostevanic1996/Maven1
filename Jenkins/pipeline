@Library('jenkins-library') _
pipeline{

    agent any
    stages{

        stage('mvn install'){

            steps{

                withMaven{
                   sh 'mvn install'
                   //installMvn.install()
                   //echo installMvn.install()
                }
            }
        }
    }
}