@Library('jenkins-library') _
pipeline{

    agent any
    stages{

        stage('mvn install'){

            steps{

                script{
                   sh 'cd'
                   sh 'cd /home/vstevanic/Maven1'
                   sh 'mvn install'
                   //installMvn.install()
                   //echo installMvn.install()
                }
            }
        }
    }
}