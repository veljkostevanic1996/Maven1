pipeline{

    agent any
    stages{

        stage('mvn install'){

            steps{
                sh 'cd'
                sh 'sudo /home/vstevanic/Maven1'
                sh 'sudo mvn clear install'
            }
        }
    }
}