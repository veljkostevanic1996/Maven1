@Library('jenkins-library') _
pipeline{

    agent any
    stages{

        stage('mvn install'){

            steps{

                withMaven{
                   sh 'mvn clean install'
                   //installMvn.install()
                   //echo installMvn.install()
                }
            }
        }
    }
}