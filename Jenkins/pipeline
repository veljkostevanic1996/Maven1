@Library('jenkins-library') _
pipeline{

    agent any
    stages{

        stage('mvn install'){

            steps{

                withMaven{

                   installMvn.install()
                   //echo installMvn.install()
                }
            }
        }
    }
}