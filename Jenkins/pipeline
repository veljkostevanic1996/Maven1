@Library('jenkins-library') _
pipeline{

    agent any
    stages{

        stage('mvn install'){

            steps{

                script{

                   installMvn.install()
                   //echo installMvn.install()
                }
            }
        }
    }
}