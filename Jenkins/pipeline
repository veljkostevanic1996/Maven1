@Library('jenkins-library') _
pipeline{

    agent any
    stages{

        stage('clone'){

            git clone 'https://username:veljkostevanic1996:Veljkostevanic123/veljkostevanic1996/Maven1.git' as 'copy-Maven1'
        }
        stage('mvn install'){

            steps{

                script{
                   sh 'cd'
                   sh 'cd /home/vstevanic/Maven1'
                   sh 'mvn clean install'
                   //installMvn.install()
                }
            }
        }
    }
}